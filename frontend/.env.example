# Site Configuration
NEXT_PUBLIC_SITE_URL=https://blogg.ing

# Database Configuration
# Set to 'false' to use real Supabase, 'true' for mock data
USE_MOCK_DATA=true

# Supabase Configuration (only needed when USE_MOCK_DATA=false)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-key-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Analytics (optional - Plausible Analytics already configured via custom script)
# This is for reference only - the script is already embedded in layout.tsx
