name = "blogging-summarizer"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Cron trigger - runs daily at 4:00 AM UTC (1 hour after crawler)
[triggers]
crons = ["0 4 * * *"]

# Environment variables (secrets set via CLI)
# SUPABASE_URL
# SUPABASE_SERVICE_KEY
# OPENAI_API_KEY
